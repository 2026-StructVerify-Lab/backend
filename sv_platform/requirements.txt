# sv_platform 의존성
# (structverify 라이브러리 의존성과 별개로 플랫폼에서만 필요한 것들)

# ── FastAPI 본체 ──
fastapi>=0.110.0
uvicorn[standard]>=0.27.0
python-multipart>=0.0.9          # 파일 업로드 (Phase 3)

# ── 설정 ──
pydantic>=2.6.0
pydantic-settings>=2.2.0
pydantic[email]>=2.6.0           # EmailStr 검증
email-validator>=2.1.0
python-dotenv>=1.0.0

# ── DB ──
sqlalchemy[asyncio]>=2.0.25
asyncpg>=0.29.0                  # FastAPI에서 async 호출용
psycopg2-binary>=2.9.9           # Alembic (sync) 용
alembic>=1.13.0

# ── 인증/보안 ──
python-jose[cryptography]>=3.3.0  # JWT
passlib[argon2]>=1.7.4
argon2-cffi>=23.1.0

# ── HTTP (라이브러리도 씀) ──
httpx>=0.27.0

# ── 비동기 잡 (Phase 2) ──
arq>=0.25.0
redis>=5.0.0

# ── 문서 로더 (Phase 3) ──
pdfplumber>=0.10.0
python-docx>=1.1.0
trafilatura>=1.6.0
