# ── Python ──
__pycache__/
*.py[cod]
*.egg-info/
dist/
build/
.venv/
venv/

# ── Environment ──
.env**
.env.local
.env.*.local
.env

# zip 으로 끝나는 모든 것 제외
*.zip
# .csv 로 끝나는 모든 것 제외
*.csv

# Example 제외
*.env.example
# 모든 폴더 안에 있는 env.example 제외
*/*.env.example
**.env.example
# [TODO-all]
# 본인 개인 개발 환경 env 를 추가했다면 여기 밑에 추가해주세요.


# ── IDE ──
.vscode/
.idea/
.cursor/
*.swp
*.swo
.DS_Store
*.DS_Store



# ── Logs ──
*.log
logs/

# ── OS ──
Thumbs.db

# -- config 파일 --
config/default.yaml

kosis_catalog_cache.json
/*.csv

# result files
*.csv
*.zip

# python
__pycache__/
*.pyc

# env
.env
venv/

createTable.py

# agent loop의 잡별 산출물 (workspace) — 로컬 캐시일 뿐 버전 관리 대상 아님
agent_workspace/
# 작업 중 만든 백업 폴더 (날짜 스탬프 — 임시)
.backup_*/

# 테스트용 article 데이터 (검증 입력, 사용자 로컬에서만 유지)
articles.jsonl

# ── 블로그 글 (티스토리 발행용 초안 — 코드 아님, 버전 관리 제외) ──
blog/

# ── Claude Code / 에이전트 세션 데이터 ──
.claude/

# ── pytest 캐시 ──
.pytest_cache/

# ── 테스트 산출물 (생성물) ──
test_outputs/

# ── 개인 메모 (태그 정리 등 — 로컬에서만) ──
태그
/eval/datasets
/eval/runs
