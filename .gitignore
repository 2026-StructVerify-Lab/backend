# ── Python ──
__pycache__/
*.py[cod]
*.egg-info/
dist/
build/
.venv/
venv/

# ── Environment ──
.env**
.env.local
.env.*.local
.env

# zip 으로 끝나는 모든 것 제외
*.zip
# .csv 로 끝나는 모든 것 제외
*.csv

# Example 제외
*.env.example
# 모든 폴더 안에 있는 env.example 제외
*/*.env.example
**.env.example
# [TODO-all]
# 본인 개인 개발 환경 env 를 추가했다면 여기 밑에 추가해주세요.


# ── IDE ──
.vscode/
.idea/
.cursor/
*.swp
*.swo
.DS_Store
*.DS_Store



# ── Logs ──
*.log
logs/

# ── OS ──
Thumbs.db

# -- config 파일 --
config/default.yaml

kosis_catalog_cache.json
/*.csv

# result files
*.csv
*.zip

# python
__pycache__/
*.pyc

# env
.env
venv/

createTable.py

# agent loop의 잡별 산출물 (workspace) — 로컬 캐시일 뿐 버전 관리 대상 아님
agent_workspace/

# ── StructVerify eval (로컬 생성물, GitHub 미추적) ──
# eval/README.md, eval/builder/ 등은 추적. 아래만 제외.
eval/datasets/**
!eval/datasets/.gitkeep
eval/runs/**
!eval/runs/.gitkeep